A sketch for flac decoder.

Currently it can read flac metadata and parse streaminfo metadata block (use #'flac:open-flac).
Also it can read frames with #'flac:frame-reader and decode them with #'flac:frame-decode

Frame and streaminfo data can be found in corresponding classes
